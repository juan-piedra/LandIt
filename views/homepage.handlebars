<section>
    <div class="title-container">Pick Your Style!</div>
    <div class="grid-container">
        {{#each styles}}
        <a href="/{{name}}"><button class="box">{{name}}</button></a>
        {{/each}}
    </div>
</section>